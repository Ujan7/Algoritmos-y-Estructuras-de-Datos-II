#include <stdio.h>
#include <stdlib.h>

size_t string_length(const char *str);
/*Indicates the size of the char*/

char *string_filter(const char *str, char c);
/*Returns the char without the characters that are equal to c*/